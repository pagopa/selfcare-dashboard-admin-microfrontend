# Copy this file to .env.local (gitignored) for personal dev overrides.
# Values in .env.local take precedence over .env when running locally.

# Use mocked API responses instead of real endpoints
VITE_API_MOCK_REQUEST_DATA=true
